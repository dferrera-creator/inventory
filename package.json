{
  "name": "inventory-app",
  "version": "1.0.0",
  "description": "Inventory management app with PostgreSQL on Railway",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:init": "node scripts/initDatabase.js",
    "db:migrate": "node scripts/migrate.js",
    "setup-db": "npm run db:init && npm run db:migrate",
    "build": "echo 'Build complete. Database will initialize on release.'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "body-parser": "^1.20.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
